--- a/src/Makefile
+++ b/src/Makefile
@@ -311,7 +311,7 @@ ifeq ($(OS),Unix)
 	@echo "**"
 	@echo "** Generic unix configuration."
 	@echo "**"
-	$(MAKE) linux gimp lib  # zart
+	$(MAKE) linux gimp lib zart
 else
 ifeq ($(OS),Darwin)
 	@echo "**"
