--- a/src/Makefile
+++ b/src/Makefile
@@ -291,7 +291,7 @@ ifeq ($(OS),Unix)
 	@echo "**"
 	@echo "** Generic unix configuration."
 	@echo "**"
-	$(MAKE) linux gimp lib  # zart
+	$(MAKE) linux gimp lib  zart
 else
 ifeq ($(OS),Darwin)
 	@echo "**"
